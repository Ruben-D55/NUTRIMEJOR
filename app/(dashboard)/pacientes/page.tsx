"use client";import{useState}from"react";import Link from"next/link";import{Clock3,Edit3,Plus,Search,Trash2,Users}from"lucide-react";import{fullName,imc}from"@/lib/patients";import{usePatients}from"@/components/patients/patient-provider";export default function Page(){const{patients,ready,remove}=usePatients(),[q,setQ]=useState("");const rows=patients.filter(p=>`${fullName(p)} ${p.document}`.toLowerCase().includes(q.toLowerCase()));function del(id:string,name:string){if(confirm(`¿Eliminar definitivamente a ${name}?`))remove(id)}return <section><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-emerald-700">Pacientes</p><h1 className="mt-2 font-display text-3xl font-bold">Mis pacientes</h1><p className="mt-2 text-slate-500">Consulta y administra todas las fichas.</p></div><Link href="/pacientes/nuevo" className="inline-flex items-center justify-center gap-2 rounded-xl bg-emerald-700 px-5 py-3 text-sm font-semibold text-white"><Plus className="h-4 w-4"/>Nuevo paciente</Link></div><div className="card mt-6 overflow-hidden"><div className="flex flex-col gap-3 border-b p-5 dark:border-emerald-800 sm:flex-row sm:items-center"><div className="relative max-w-md flex-1"><Search className="absolute left-3 top-3 h-5 w-5 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar por nombre o documento" className="h-11 w-full rounded-xl border bg-transparent pl-11 pr-4"/></div><span className="text-sm text-slate-500">{rows.length} pacientes</span></div><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-400 dark:bg-emerald-950"><tr><th className="p-4">Paciente</th><th>Contacto</th><th>Objetivo</th><th>IMC</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{ready&&rows.map(p=><tr className="border-t dark:border-emerald-800" key={p.id}><td className="p-4"><strong>{fullName(p)}</strong><small className="block text-slate-500">CI {p.document||'—'}</small></td><td>{p.phone||'—'}<small className="block text-slate-500">{p.email}</small></td><td className="max-w-52 truncate">{p.objective||'Sin definir'}</td><td className="font-semibold">{imc(p)}</td><td><span className="rounded-full bg-emerald-100 px-3 py-1 text-xs font-bold text-emerald-700">{p.status}</span></td><td><div className="flex gap-1"><Link title="Historial" href={`/pacientes/${p.id}/historial`} className="rounded-lg p-2 hover:bg-slate-100 dark:hover:bg-emerald-950"><Clock3 className="h-4 w-4"/></Link><Link title="Editar" href={`/pacientes/${p.id}/editar`} className="rounded-lg p-2 hover:bg-slate-100 dark:hover:bg-emerald-950"><Edit3 className="h-4 w-4"/></Link><button title="Eliminar" onClick={()=>del(p.id,fullName(p))} className="rounded-lg p-2 text-red-500 hover:bg-red-50"><Trash2 className="h-4 w-4"/></button></div></td></tr>)}</tbody></table>{ready&&!rows.length&&<div className="grid min-h-64 place-items-center text-center text-slate-400"><div><Users className="mx-auto mb-3 h-10 w-10"/><p>No se encontraron pacientes.</p></div></div>}</div></div></section>}
