"use client";import{useParams}from"next/navigation";import{PatientForm}from"@/components/patients/patient-form";import{usePatients}from"@/components/patients/patient-provider";export default function Page(){const{id}=useParams<{id:string}>(),{patients,ready}=usePatients(),p=patients.find(x=>x.id===id);if(!ready)return <p>Cargando…</p>;if(!p)return <div className="card p-8">Paciente no encontrado.</div>;return <section><p className="text-xs font-bold uppercase tracking-widest text-emerald-700">Pacientes</p><h1 className="mt-2 font-display text-3xl font-bold">Editar paciente</h1><p className="mt-2 text-slate-500">Actualiza la información de la ficha.</p><PatientForm patient={p}/></section>}
