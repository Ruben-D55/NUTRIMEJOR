"use client";import{useParams}from"next/navigation";import{useState}from"react";import{CalendarClock,Plus}from"lucide-react";import{fullName}from"@/lib/patients";import{usePatients}from"@/components/patients/patient-provider";export default function Page(){const{id}=useParams<{id:string}>(),{patients,ready,addHistory}=usePatients(),[type,setType]=useState("Control"),[notes,setNotes]=useState(""),p=patients.find(x=>x.id===id);if(!ready)return <p>Cargando…</p>;if(!p)return <div className="card p-8">Paciente no encontrado.</div>;function submit(e:React.FormEvent){e.preventDefault();if(!notes.trim())return;addHistory(id,type,notes);setNotes("")}return <section><p className="text-xs font-bold uppercase tracking-widest text-emerald-700">Historial clínico</p><h1 className="mt-2 font-display text-3xl font-bold">{fullName(p)}</h1><p className="mt-2 text-slate-500">Consultas, controles y cambios importantes.</p><div className="mt-6 grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><form onSubmit={submit} className="card h-fit p-6"><h2 className="font-display text-lg font-bold">Nueva anotación</h2><label className="mt-5 grid gap-2 text-sm font-semibold">Tipo<select value={type} onChange={e=>setType(e.target.value)} className="h-11 rounded-xl border bg-transparent px-3"><option>Control</option><option>Evaluación</option><option>Plan nutricional</option><option>Observación</option></select></label><label className="mt-4 grid gap-2 text-sm font-semibold">Notas<textarea value={notes} onChange={e=>setNotes(e.target.value)} rows={5} className="rounded-xl border bg-transparent p-3" required/></label><button className="mt-5 inline-flex items-center gap-2 rounded-xl bg-emerald-700 px-5 py-3 text-sm font-semibold text-white"><Plus className="h-4 w-4"/>Agregar al historial</button></form><div className="card p-6"><h2 className="font-display text-lg font-bold">Línea de tiempo</h2><div className="mt-5 space-y-5">{p.history.map(h=><article className="relative border-l-2 border-emerald-200 pl-6" key={h.id}><span className="absolute -left-3 grid h-6 w-6 place-items-center rounded-full bg-emerald-100 text-emerald-700"><CalendarClock className="h-3 w-3"/></span><div className="flex items-center justify-between gap-3"><strong>{h.type}</strong><time className="text-xs text-slate-500">{new Date(h.date).toLocaleDateString('es-BO')}</time></div><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">{h.notes}</p></article>)}</div></div></div></section>}
