import{z}from"zod";export const credentials=z.object({email:z.string().email().max(180).transform(x=>x.toLowerCase()),password:z.string().min(8).max(100)});export const register=credentials.extend({name:z.string().trim().min(3).max(120)});export const patient=z.object({names:z.string().trim().min(2).max(100),lastNames:z.string().trim().min(2).max(100),document:z.string().max(30).optional().default(""),birthDate:z.string().optional().default(""),sex:z.string().max(20).optional().default(""),phone:z.string().max(30).optional().default(""),email:z.union([z.string().email(),z.literal("")]).optional().default(""),objective:z.string().max(300).optional().default(""),weight:z.coerce.number().min(0).max(500),height:z.coerce.number().min(0).max(300),status:z.enum(["Activo","Inactivo"])});
